{% set name = "click" %}
{% set version = "7.0" %}

package:
  name: my_k_means
  version: 0

source:
  url: 
  sha256: 

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  host:
    - pip
    - python
  run:
    - python

test:
  imports:
    - click

about:
  license: None
  license_family: None
  summary: k means implemented in Python